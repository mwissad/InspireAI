export default function DatabricksLogo({ className = 'w-8 h-8' }) {
  return (
    <img
      src="/databricks-logo.png?v=9"
      alt="Inspire AI"
      className={`${className} rounded`}
    />
  );
}
